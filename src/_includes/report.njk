<!DOCTYPE html>
<html lang="ja">

<head>
    <base target="_top">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | {{ site.company }}</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600;700&family=Noto+Sans+JP:wght@300;400;500;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/assets/unified_design_system.css">
    <style>
        /* Markdown変換コンテンツ用の追加スタイル */
        .report-content ul,
        .report-content ol {
            margin: var(--spacing-md) 0;
            padding-left: var(--spacing-xl);
            line-height: 1.8;
        }

        .report-content li {
            margin-bottom: var(--spacing-sm);
        }

        .report-content blockquote {
            background: var(--color-bg-lighter);
            border-left: 4px solid var(--color-accent-blue);
            padding: var(--spacing-lg);
            border-radius: 0 var(--radius-lg) var(--radius-lg) 0;
            margin: var(--spacing-lg) 0;
            color: var(--color-navy-main);
        }

        .report-content code {
            background: var(--color-bg-lighter);
            padding: 0.15em 0.4em;
            border-radius: var(--radius-sm);
            font-family: var(--font-family-mono);
            font-size: var(--font-size-sm);
        }

        .report-content pre {
            background: var(--color-navy-darkest);
            color: var(--color-slate-lightest);
            padding: var(--spacing-lg);
            border-radius: var(--radius-lg);
            overflow-x: auto;
            margin: var(--spacing-lg) 0;
        }

        .report-content pre code {
            background: none;
            padding: 0;
            color: inherit;
        }

        .report-content hr {
            border: none;
            border-top: 2px solid var(--color-border);
            margin: var(--spacing-2xl) 0;
        }

        .report-content img {
            max-width: 100%;
            height: auto;
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
            margin: var(--spacing-lg) 0;
        }
    </style>
</head>

<body>
    <div class="container">
        <header style="position: static; background: none; border: none; box-shadow: none; margin-bottom: var(--spacing-xl);">
            <nav class="breadcrumb">
                <a href="/index.html">ポータルトップ</a>
                <span>/</span>
                <span>{{ title }}</span>
            </nav>
            <h1>{{ title }}</h1>
            {% if subtitle %}<p class="text-muted" style="font-size: var(--font-size-lg);">{{ subtitle }}</p>{% endif %}
        </header>

        <main class="report-content">
            {{ content | safe }}
        </main>

        <footer style="margin-top: var(--spacing-3xl); padding-top: var(--spacing-xl); border-top: 1px solid var(--color-border); text-align: center;">
            <p class="text-muted" style="font-size: var(--font-size-sm);">
                {{ site.company }} 情報システム部
            </p>
        </footer>
    </div>
</body>

</html>
